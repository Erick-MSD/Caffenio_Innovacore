# PNPM Configuration
# Configuración para el proyecto Caffenio

# Hoist dependencies to the root node_modules
shamefully-hoist=false

# Strict peer dependencies
strict-peer-dependencies=false

# Store directory (opcional - descomenta si quieres usar un store local)
# store-dir=.pnpm-store

# Auto install peers
auto-install-peers=true

# Lockfile settings
lockfile=true
